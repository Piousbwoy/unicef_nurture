# ONNX weights travel as plain git blobs. GitHub's LFS quota is exhausted on
# this remote, and every shipped model is under the 100 MB per-file hard limit.
# `binary` keeps git from diffing, merging, or newline-converting them.
*.onnx binary
