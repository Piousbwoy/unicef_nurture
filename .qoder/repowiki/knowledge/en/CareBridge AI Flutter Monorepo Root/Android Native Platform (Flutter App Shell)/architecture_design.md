This is a standard Flutter-generated Android project with two Gradle modules: the root `build.gradle.kts` configures shared repositories and redirects all build output to a sibling `build/` directory, while `settings.gradle.kts` includes the Flutter SDK's gradle tooling (`includeBuild`) and declares the single `:app` module. The `app/` module applies `com.android.application` and `dev.flutter.flutter-gradle-plugin`, sets `namespace = "gh.carebridge.carebridge_ai"`, and points `flutter { source = "../.." }` back to the Flutter project root. `MainActivity.kt` extends `FlutterActivity` as the sole Kotlin entry point; `GeneratedPluginRegistrant.java` under `io.flutter.plugins` is auto-generated by Flutter to wire plugins. `AndroidManifest.xml` declares the launcher activity, Flutter embedding v2 metadata, and the `queries` block for text processing. Build types default to debug signing for release builds, and Gradle properties enable AndroidX with legacy DSL flags.