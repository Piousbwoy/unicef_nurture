{{flutter_js}}
{{flutter_build_config}}
// App shell caching belongs to app_sw.js, never the generated Flutter worker.
_flutter.loader.load({config: {
  canvasKitBaseUrl: 'canvaskit/',
  fontFallbackBaseUrl: 'font_fallbacks/'
}});
